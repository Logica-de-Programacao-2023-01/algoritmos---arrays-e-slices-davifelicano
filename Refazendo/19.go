// Faça um programa que leia dois arrays de inteiros de tamanho n
// e gere um terceiro array que seja a soma dos dois primeiros.
package main

import "fmt"

func main() {
	var tamanho, x int
	fmt.Print("Informe o tamanho do array,valendo para os dois arrays:")
	fmt.Scan(&tamanho)
	fmt.Print("Informe números para serem adicinonados no array")
	Array := [tamanho]int{}
	Array2 := [tamanho]int{x}
	for len(Array) <= len(Array2) {
		fmt.Print("Infor")
	}

}
